# Панель выкладывается целиком из web/src — здесь не должно быть ничего лишнего.
config.json
tokens.json
.env
.env.*
# архив расширения нужен на сайте, поэтому запрет снят
!setup/reseller-funpay-ext.zip
